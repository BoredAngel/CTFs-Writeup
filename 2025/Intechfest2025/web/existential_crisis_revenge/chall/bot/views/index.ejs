<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Peak Existential Crisis</title>
    <link href="bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="style.css" />
    <style>
      .audio-player {
        position: absolute;
        bottom: 1rem;
        left: 1rem;
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
        background: rgba(0, 0, 0, 0.7);
        padding: 1rem;
        border: 2px solid #ff0000;
        border-radius: 0;
      }
      .audio-player label {
        font-size: 0.9rem;
        color: white;
      }
      .audio-player2 {
        position: absolute;
        bottom: 1rem;
        right: 1rem;
        background: rgba(0, 0, 0, 0.7);
        padding: 1rem;
        border: 2px solid #ff0000;
        border-radius: 0;
      }
      .audio-player2 label {
        font-size: 0.9rem;
        color: white;
        display: block;
        margin-bottom: 0.5rem;
      }
    </style>
  </head>
  <body
    class="bg-dark text-white d-flex align-items-center justify-content-center vh-100 persona-bg"
  >
    <div class="text-center">
      <h1 id="personaTitle" class="persona-title mb-4"></h1>
      <input
        type="text"
        class="form-control mb-3"
        id="urlInput"
        placeholder="http://web-existential-crisis-app:1336"
      />
      <!-- csrf token -->
      <input type="hidden" id="csrfToken" value="<%= csrfToken %>" />
      <button class="btn persona-btn" id="visitBtn">Visit</button>
      <div
        id="statusModal"
        class="modal fade show"
        tabindex="-1"
        style="display: none"
        aria-modal="true"
        role="dialog"
      >
        <div class="modal-dialog modal-dialog-centered">
          <div class="modal-content bg-dark text-white border-danger">
            <div class="modal-header border-bottom border-danger">
              <h5 class="modal-title text-danger">🕵️ Running Monitoring...</h5>
            </div>
            <div class="modal-body">
              <p id="modalStatus">Initializing...</p>
              <div class="progress mt-3">
                <div
                  id="modalProgressBar"
                  class="progress-bar progress-bar-striped progress-bar-animated bg-danger"
                  style="width: 25%"
                ></div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="audio-player">
      <label>🎵 Beneath The Mask</label>
      <audio id="beneathTheMask" src="p5-mus.mp3" loop controls></audio>
      <label>🎶 No-voice version</label>
      <audio id="instrumental" src="p5-inst.mp3" loop controls></audio>
    </div>

    <div class="audio-player2">
      <label>🎵 Color Your Night</label>
      <audio id="colorYourNight" src="p3.mp3" loop controls></audio>
    </div>

    <audio id="click-sfx" src="click.mp3"></audio>

    <script src="index.js"></script>
  </body>
</html>
